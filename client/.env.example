# Базовий шлях API (вживається як axios baseURL).
# За замовчуванням /api — у dev-режимі Vite проксує на http://localhost:8000.
# Для продакшену можна вказати повний URL, напр. https://api.example.com/api

VITE_API_BASE_PATH=/api
